apiVersion: v1
kind: Service
metadata:
  name: agiworkforce-service
  namespace: default
  labels:
    app: agiworkforce-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: agiworkforce-app
